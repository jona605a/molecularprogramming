crn = {
    conc[a,64],
    conc[b,20],
    step[{
        ld[a,atmp],
        ld[b,btmp]
    }],
    step[{
        cmp[a,b]
    }],
    step[{
        ifGT[{ sub[atmp,btmp,a] }],
        ifLT[{ sub[btmp,atmp,b] }]
    }]
}
